// Condition Challenge Q2 - Comparing Strings
// Objective: Check if two strings are equal

// Declare first string
let str1 = "Rishit";
// Declare second string with the same value
let str2 = "Rishit";

// if-else statement: Check string equality
// === operator: Strict equality (checks both value AND type)
if(str1===str2){
    // This block executes if both strings are exactly equal
    console.log("They are equal")
    // Output: They are equal
}else{
    // This block executes if strings are not equal
    console.log("They are not")
}